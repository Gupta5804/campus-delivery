FROM node:alpine

# CMD [ "mkdir", "/app/www/" ]

WORKDIR /app/www

COPY ./apps/www/ .

RUN echo $NEXT_PUBLIC_HOST 
RUN echo $NODE_ENV 
RUN npm install
ENV NEXT_PUBLIC_HOST=$NEXT_PUBLIC_HOST
ENV NODE_ENV=$NODE_ENV
RUN npm run build

EXPOSE 3000

CMD [ "npm", "start" ]

